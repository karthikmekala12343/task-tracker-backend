-- Create database for the Task Tracker backend
CREATE DATABASE IF NOT EXISTS tasktracker;

-- Optional: create a dedicated user if you want
-- CREATE USER 'tasktracker_user'@'localhost' IDENTIFIED BY 'your_password';
-- GRANT ALL PRIVILEGES ON tasktracker.* TO 'tasktracker_user'@'localhost';
-- FLUSH PRIVILEGES;
